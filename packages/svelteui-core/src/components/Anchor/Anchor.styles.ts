import { createStyles } from '$lib/styles-old';

export default createStyles(() => {
	return {
		root: {
			backgroundColor: 'transparent',
			cursor: 'pointer',
			padding: 0,
			border: 0
		}
	};
});
