---
id: 'use-focus'
title: 'use-focus'
group: 'svelteuidev-actions'
packageGroup: '@svelteuidev/actions'
description: 'Calls focus on a node once mounted into the dom'
example: 'https://codesandbox.io/embed/use-focus-y0s9ii?fontsize=14&hidenavigation=1&module=%2FApp.svelte&theme=dark&view=preview'
---

## Usage

<p>
	With the <strong>{meta.title}</strong> action, the affected dom node gets focused when it is
	mounted into the dom. Only "focusable" elements should use this action. Type errors will appear if
	this is not the case.
</p>

```tsx
<script>
    import { Button } from '@svelteuidev/core';
    import { focus } from '@svelteuidev/actions';

    let name = 'world';
    let editing = false;

    function toggleEdit() {
        editing = !editing;
    }
</script>

<p>Name: {name}</p>
    {#if editing}
        <label>
            Name
            <input use:focus type="text" bind:value={name} />
        </label>
    {/if}
<Button on:click={toggleEdit}>{editing ? 'Confirm' : 'Edit'}</Button>
```

## Params

| Param | Description |
| ----- | ----------- |
| none  | N/A         |

## Events

<p>
	The <strong>{meta.title}</strong> action does not dispatch any custom events`
</p>

```ts
// No events to display
```

## Definition

```ts
export function focus(node: FocusableElement): ReturnType<Action>;
```
